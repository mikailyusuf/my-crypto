export interface CryptoModel {
    id: string;           
    name: string;         
    symbol: string;        
    current_price: number; 
    image:string,
    price_change_percentage_24h: number;
  }